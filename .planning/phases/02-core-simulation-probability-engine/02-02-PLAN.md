---
phase: 02-core-simulation-probability-engine
plan: 02-02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - backend/app/simulation/modifiers.py
  - backend/app/simulation/simulator.py
  - backend/tests/test_simulation/test_modifiers.py
autonomous: true
must_haves:
  truths:
    - "SimulationModifier interface protocol is correctly defined"
    - "Powerplay, Required Run Rate, Momentum, and Bowling Pressure modifiers are implemented and follow specified math equations"
    - "Modifier pipeline successfully applies modifications sequentially and normalizes the outcome vector"
  artifacts:
    - path: "backend/app/simulation/modifiers.py"
      provides: "Classes for PowerplayModifier, RequiredRunRateModifier, MomentumModifier, and BowlingPressureModifier"
---

<objective>
Implement the Strategy Pattern modifier interface and build four composable modifier strategies: Powerplay aggression, Required Run Rate chase pressure, exponential batting momentum decay, and dot-ball bowling pressure stacking.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-simulation-probability-engine/02-CONTEXT.md
@.planning/phases/02-core-simulation-probability-engine/02-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Define SimulationModifier protocol and integrate into InningsSimulator</name>
  <files>backend/app/simulation/modifiers.py, backend/app/simulation/simulator.py</files>
  <read_first>backend/app/simulation/simulator.py</read_first>
  <action>
    Create `backend/app/simulation/modifiers.py` and define the `SimulationModifier` protocol:
    - Interface requires `apply(state: MatchState, probs: np.ndarray) -> np.ndarray`.
    Modify `InningsSimulator` in `backend/app/simulation/simulator.py` to:
    - Maintain a list of modifiers in `self.modifiers`.
    - Apply modifiers sequentially in the simulation loop, then normalize and clip the final probabilities.
  </action>
  <acceptance_criteria>
    - modifiers.py defines SimulationModifier protocol
    - InningsSimulator imports modifiers and applies them sequentially during resolution
  </acceptance_criteria>
  <verify>python -c "import sys; sys.path.append('backend'); from app.simulation.modifiers import SimulationModifier; print(SimulationModifier)"</verify>
  <done>SimulationModifier interface protocol defined and registered in simulator</done>
</task>

<task type="auto">
  <name>Implement Powerplay and Required Run Rate modifiers</name>
  <files>backend/app/simulation/modifiers.py</files>
  <read_first>backend/app/simulation/modifiers.py</read_first>
  <action>
    Write the following modifier implementations in `backend/app/simulation/modifiers.py`:
    - `PowerplayModifier`: Checks if total legal balls < 36. Boosts FOUR by 1.25, SIX by 1.15, and reduces DOT by 0.90.
    - `RequiredRunRateModifier`: Checks if `state.target` is not None and legal balls < 120. Calculate RRR = (runs_needed / balls_remaining) * 6. If RRR > 8.0, calculate `aggression_factor = min((rrr - 8.0) * 0.08, 0.5)`. Multiply SIX by `(1.0 + aggression_factor)`, FOUR by `(1.0 + aggression_factor * 0.5)`, WICKET by `(1.0 + aggression_factor * 0.8)`, and DOT by `(1.0 - aggression_factor * 0.6)`.
  </action>
  <acceptance_criteria>
    - PowerplayModifier scales boundary and dot outcomes correctly in the powerplay
    - RequiredRunRateModifier scales batsman risk/reward profile non-linearly under high chase run rates
  </acceptance_criteria>
  <verify>python -c "import sys; sys.path.append('backend'); from app.simulation.modifiers import PowerplayModifier, RequiredRunRateModifier; print(PowerplayModifier, RequiredRunRateModifier)"</verify>
  <done>Powerplay and Required Run Rate modifiers implemented and verified</done>
</task>

<task type="auto">
  <name>Implement Momentum and Bowling Pressure modifiers</name>
  <files>backend/app/simulation/modifiers.py</files>
  <read_first>backend/app/simulation/modifiers.py</read_first>
  <action>
    Write the following modifier implementations in `backend/app/simulation/modifiers.py`:
    - `MomentumModifier`: Checks if `state.balls_since_boundary <= 3`. Calculates decay factor `0.65 ** state.balls_since_boundary`. Calculates `boost = 1.0 + 0.40 * decay_factor` (capped at 1.4) and `suppress = 1.0 - 0.30 * decay_factor` (capped at minimum 0.7). Scales FOUR and SIX by boost, and WICKET by suppress. Ensure `balls_since_boundary` is updated correctly in simulator (reset to 0 on a boundary, reset to 10 on bowler change or wicket).
    - `BowlingPressureModifier`: Checks if `state.consecutive_dots > 0`. Calculates pressure = `min(state.consecutive_dots * 0.05, 0.25)`. Scales WICKET by `(1.0 + pressure)`, and FOUR/SIX by `(1.0 - pressure * 0.5)`. Ensure `consecutive_dots` is updated in simulator (reset to 0 on any runs/extras).
  </action>
  <acceptance_criteria>
    - MomentumModifier scales up boundaries and suppresses wicket probability during momentum window
    - BowlingPressureModifier scales up wickets and suppresses boundaries after dots
  </acceptance_criteria>
  <verify>python -c "import sys; sys.path.append('backend'); from app.simulation.modifiers import MomentumModifier, BowlingPressureModifier; print(MomentumModifier, BowlingPressureModifier)"</verify>
  <done>Momentum and Bowling Pressure modifiers implemented and verified</done>
</task>

<task type="auto">
  <name>Implement unit tests for Strategy modifiers</name>
  <files>backend/tests/test_simulation/test_modifiers.py</files>
  <read_first>backend/tests/test_simulation/test_modifiers.py</read_first>
  <action>
    Update `backend/tests/test_simulation/test_modifiers.py` to:
    - Verify `PowerplayModifier` applies modifications only when overs < 6.
    - Verify `RequiredRunRateModifier` increases boundary and wicket chances as RRR increases.
    - Verify `MomentumModifier` decays exponentially as balls_since_boundary increases and resets on bowler change or wicket.
    - Verify `BowlingPressureModifier` increases wicket probability as consecutive dot count increases and resets on runs.
    Run the modifier unit tests to ensure they are green.
  </action>
  <acceptance_criteria>
    - test_modifiers.py contains tests for all 4 modifier types
    - Running pytest on test_modifiers.py succeeds
  </acceptance_criteria>
  <verify>uv run pytest backend/tests/test_simulation/test_modifiers.py</verify>
  <done>Unit tests for modifiers written and verified green</done>
</task>
</tasks>

<verification>
- [ ] uv run pytest backend/tests/test_simulation/test_modifiers.py succeeds
</verification>

<success_criteria>
- Modifiers correctly apply context-dependent probability shifts matching the mathematical design
- Tests demonstrate proper scaling, decay, reset rules, and bounds verification
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-simulation-probability-engine/02-02-SUMMARY.md`
</output>
