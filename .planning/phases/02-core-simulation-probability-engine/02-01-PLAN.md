---
phase: 02-core-simulation-probability-engine
plan: 02-01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/simulation/models.py
  - backend/app/simulation/simulator.py
  - backend/tests/test_simulation/test_simulator.py
  - backend/tests/test_simulation/test_modifiers.py
  - backend/tests/test_simulation/test_calibration.py
autonomous: true
must_haves:
  truths:
    - "MatchOutcome enum, MatchState, and InningsResult data models are correctly defined"
    - "Base matchup vector synthesis properly blends batter and bowler percentiles with non-linear scaling and tailender penalties"
    - "InningsSimulator simulates a full 20-over innings ball-by-ball with correct strike rotation, bowler allocations, and extras"
  artifacts:
    - path: "backend/app/simulation/models.py"
      provides: "Match outcome enum, match state dataclass, innings result dataclass, and base matchup synthesis logic"
    - path: "backend/app/simulation/simulator.py"
      provides: "InningsSimulator class and core Monte Carlo simulation loop"
---

<objective>
Define core simulation data structures, implement batter-bowler percentile blending math with tailender constraints, and build the ball-by-ball Monte Carlo innings simulation loop (bowler selection, strike rotation, extras, wickets, and chase targets).
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-simulation-probability-engine/02-CONTEXT.md
@.planning/phases/02-core-simulation-probability-engine/02-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Initialize Wave 0 test stubs</name>
  <files>
    backend/tests/test_simulation/test_simulator.py
    backend/tests/test_simulation/test_modifiers.py
    backend/tests/test_simulation/test_calibration.py
  </files>
  <read_first>backend/pyproject.toml</read_first>
  <action>
    Create the `backend/tests/test_simulation/` directory.
    Write basic test stubs to:
    - `backend/tests/test_simulation/test_simulator.py`: Stub verifying test runner execution.
    - `backend/tests/test_simulation/test_modifiers.py`: Stub verifying modifier structure tests.
    - `backend/tests/test_simulation/test_calibration.py`: Stub verifying calibration test suite existence.
    Run `uv run pytest backend/tests/test_simulation/` to ensure the test runner executes these stubs.
  </action>
  <acceptance_criteria>
    - test_simulator.py exists
    - test_modifiers.py exists
    - test_calibration.py exists
    - Pytest execution finishes with all stubs passing
  </acceptance_criteria>
  <verify>uv run pytest backend/tests/test_simulation/</verify>
  <done>Wave 0 test stubs created and verified green</done>
</task>

<task type="auto">
  <name>Implement simulation data models and percentile blending</name>
  <files>backend/app/simulation/models.py</files>
  <read_first>backend/app/models/player.py</read_first>
  <action>
    Create `backend/app/simulation/models.py` with:
    - `MatchOutcome` IntEnum: DOT=0, ONE=1, TWO=2, THREE=3, FOUR=4, SIX=6, WICKET=7, WIDE=8, NO_BALL=9.
    - `LEAGUE_BASE_PROBS` mapping the historical baseline probabilities (DOT: 0.35, ONE: 0.38, TWO: 0.06, THREE: 0.005, FOUR: 0.11, SIX: 0.04, WICKET: 0.045, WIDE: 0.008, NO_BALL: 0.002).
    - `MatchState` dataclass to track game states (runs, wickets, overs_completed, balls_completed, target, balls_since_boundary, consecutive_dots, last_event_was_wicket).
    - `InningsResult` dataclass to represent the outcome of an innings (total_runs, wickets, overs_bowled, extras, delivery_log).
    - `synthesize_base_matchup(batter_percentile: int, bowler_percentile: int, batter_role: str) -> np.ndarray`: Implements the non-linear percentile blending math with relative skill indices $S_{bat}$ and $S_{bowl}$ scaling. If `batter_role == "BOWL"` and `batter_percentile < 25`, apply the tailender penalty. Normalize the final vector.
  </action>
  <acceptance_criteria>
    - models.py contains MatchOutcome enum, MatchState class, and synthesize_base_matchup function
    - synthesize_base_matchup blends percentiles and normalizes the output probability vector to 1.0
    - Tailender penalty reduces boundary probabilities and increases dot/wicket probabilities
  </acceptance_criteria>
  <verify>python -c "import sys; sys.path.append('backend'); from app.simulation.models import synthesize_base_matchup; p = synthesize_base_matchup(50, 50, 'BAT'); print(p.sum())"</verify>
  <done>Simulation models and base matchup synthesis implemented and verified</done>
</task>

<task type="auto">
  <name>Implement InningsSimulator core Monte Carlo loop</name>
  <files>backend/app/simulation/simulator.py</files>
  <read_first>backend/app/simulation/models.py</read_first>
  <action>
    Create `backend/app/simulation/simulator.py` with the `InningsSimulator` class:
    - Initialize with a thread-safe local generator instance `np.random.default_rng(seed)`.
    - Implement `_select_bowler(bowlers, overs_bowled_tracker, last_bowler_id)` to choose a bowler who did not bowl the last over and has bowled less than 4 overs (24 legal balls), prioritizing players with the 'BOWL' role.
    - Implement `simulate_innings(batting_lineup, bowling_lineup, target=None) -> InningsResult`:
      - Track active striker and non-striker positions.
      - Resolve outcomes using `rng.choice` on the synthesized probabilities.
      - Support strike rotation on 1 or 3 runs, and end of overs.
      - Handle extras (WIDE, NO_BALL) which do not consume legal balls, and wicket fall replacement (striker is replaced by next batsman).
      - Halt when 10 wickets fall, 20 overs are completed, or the target is reached in Innings 2.
  </action>
  <acceptance_criteria>
    - simulator.py contains InningsSimulator class
    - simulate_innings implements the ball-by-ball loop with strike rotation and bowler allocation checks
    - Innings halts immediately if wickets reach 10, overs reach 20, or target is reached
  </acceptance_criteria>
  <verify>python -c "import sys; sys.path.append('backend'); from app.simulation.simulator import InningsSimulator; print(InningsSimulator)"</verify>
  <done>InningsSimulator core Monte Carlo ball-by-ball loop implemented</done>
</task>

<task type="auto">
  <name>Implement unit tests for InningsSimulator core behaviors</name>
  <files>backend/tests/test_simulation/test_simulator.py</files>
  <read_first>backend/app/simulation/simulator.py</read_first>
  <action>
    Update `backend/tests/test_simulation/test_simulator.py` to cover:
    - Percentile blending: verifying sum of probabilities is 1.0, and high ratings favor batsman/bowler correctly.
    - Tailender penalty: verifying tailenders have heavily suppressed boundary rates.
    - Bowler allocation: verifying bowler quotas (max 4 overs/24 balls per bowler) and bowler alternation.
    - Strike rotation: verifying striker swaps on 1/3 runs, end of overs, and wickets.
    - Innings termination: verifying exit on 10 wickets, 20 overs, or target reached.
    Run the simulator tests to verify correctness.
  </action>
  <acceptance_criteria>
    - test_simulator.py contains test cases for blending, tailender penalties, bowler selection, strike rotation, and termination
    - Running pytest on test_simulator.py passes without errors
  </acceptance_criteria>
  <verify>uv run pytest backend/tests/test_simulation/test_simulator.py</verify>
  <done>Unit tests for core simulator behaviors implemented and verified green</done>
</task>
</tasks>

<verification>
- [ ] uv run pytest backend/tests/test_simulation/test_simulator.py succeeds
</verification>

<success_criteria>
- InningsSimulator successfully runs a ball-by-ball match simulation in memory under <5ms per innings
- All core cricket rules (strike rotation, bowler quotas, extras, wickets) are correctly simulated and verified via unit tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-simulation-probability-engine/02-01-SUMMARY.md`
</output>
