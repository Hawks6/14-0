---
phase: 02-core-simulation-probability-engine
plan: 02-03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - backend/app/simulation/calibrate.py
  - backend/tests/test_simulation/test_calibration.py
autonomous: true
must_haves:
  truths:
    - "Standalone CLI calibration script is created and executable"
    - "Calibration test runs 10,000 matches in isolation to verify score distributions"
    - "First-innings scores produce a mean of 157-160 (or within tolerance [154, 163]) and standard deviation of ~30 (within [25, 35])"
  artifacts:
    - path: "backend/app/simulation/calibrate.py"
      provides: "CLI script to run batch simulation calibrations and print distributions"
---

<objective>
Build a standalone CLI-testable calibration script to execute batch match simulations (10,000 runs) in isolation and write the calibration test suite to verify the statistical distributions against actual T20/IPL benchmarks.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-simulation-probability-engine/02-CONTEXT.md
@.planning/phases/02-core-simulation-probability-engine/02-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Build standalone CLI calibration script</name>
  <files>backend/app/simulation/calibrate.py</files>
  <read_first>backend/app/simulation/simulator.py</read_first>
  <action>
    Create `backend/app/simulation/calibrate.py` with:
    - A script that runs in isolation (no database connections).
    - Setup of standard dummy lineups (e.g., standard batting lineup with percentiles from 90 down to 10; bowling lineup with standard bowler ratings).
    - Functions to simulate N matches (e.g. 10,000) and compile first innings scores, wickets, boundaries (4s and 6s), extras.
    - CLI entry point parsing `--runs` (default 10,000) and printing out mean score, standard deviation, average wickets, average extras, boundary rates, and total execution time.
  </action>
  <acceptance_criteria>
    - calibrate.py exists and can be run via command line
    - Running the script runs batch simulations in memory without accessing the database
    - Execution time for 10,000 runs is under 15 seconds (<1.5ms per innings in fast mode)
  </acceptance_criteria>
  <verify>uv run python backend/app/simulation/calibrate.py --runs 100</verify>
  <done>Standalone CLI calibration script implemented and verified</done>
</task>

<task type="auto">
  <name>Implement calibration tests for statistical benchmarks</name>
  <files>backend/tests/test_simulation/test_calibration.py</files>
  <read_first>backend/app/simulation/calibrate.py</read_first>
  <action>
    Update `backend/tests/test_simulation/test_calibration.py` to:
    - Import simulation runner from calibrate.py.
    - Execute a batch of 10,000 match simulations (or a smaller statistically representative sample like 2,000 for faster pytest runs, let's stick to a robust and fast run).
    - Assert that first innings score distributions align with T20 averages:
      - Mean score is within `[154, 163]` (target 157–160).
      - Standard deviation is within `[25, 35]` (target ~30).
      - Average wickets lost is within `[5.5, 7.5]`.
    Run the calibration tests to verify compliance.
  </action>
  <acceptance_criteria>
    - test_calibration.py contains assertions for mean score, standard deviation, and wicket rates
    - Calibration tests pass reliably with correct statistical parameters
  </acceptance_criteria>
  <verify>uv run pytest backend/tests/test_simulation/test_calibration.py</verify>
  <done>Calibration tests written and verified green</done>
</task>

<task type="auto">
  <name>Verify the full backend test suite</name>
  <files>backend/tests/test_simulation/test_simulator.py, backend/tests/test_simulation/test_modifiers.py, backend/tests/test_simulation/test_calibration.py</files>
  <read_first>backend/pyproject.toml</read_first>
  <action>
    Run the entire backend test suite (`uv run pytest backend/tests/`) to ensure all tests (Phase 1 ingestion/models and Phase 2 simulation/calibration) pass cleanly and execute in under 15 seconds.
  </action>
  <acceptance_criteria>
    - All unit, integration, and calibration tests pass
    - Total test suite execution time is within the performance budget (<15s)
  </acceptance_criteria>
  <verify>uv run pytest backend/tests/</verify>
  <done>Full backend test suite executed and verified green</done>
</task>
</tasks>

<verification>
- [ ] uv run pytest backend/tests/ succeeds
</verification>

<success_criteria>
- The simulation engine is mathematically calibrated to yield realistic IPL scores (mean 157-160, SD ~30)
- Full backend test suite runs quickly and green
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-simulation-probability-engine/02-03-SUMMARY.md`
</output>
