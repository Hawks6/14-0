---
phase: 01-data-model-ingestion-engine
plan: 01-01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/app/core/database.py
  - backend/app/models/base.py
  - backend/app/models/player.py
  - backend/app/models/squad.py
  - backend/app/models/draft.py
  - backend/app/models/match.py
  - backend/alembic.ini
  - backend/alembic/env.py
autonomous: true
must_haves:
  truths:
    - "Database connects successfully via asyncpg"
    - "Declarative SQL models reflect player, franchise, season, draft, and match entities"
    - "Alembic migrations initialize and migrate the DB schema successfully"
  artifacts:
    - path: "backend/app/core/database.py"
      provides: "Database initialization, async engine, and session management"
    - path: "backend/app/models/player.py"
      provides: "SQLAlchemy models for players and player seasons"
    - path: "backend/alembic.ini"
      provides: "Alembic configuration"
---

<objective>
Configure the Python backend environment with SQLAlchemy, asyncpg, and Alembic, design the relational schema models, and set up async migration workflows.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-model-ingestion-engine/01-CONTEXT.md
@.planning/phases/01-data-model-ingestion-engine/01-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Initialize Python backend project dependencies</name>
  <files>backend/pyproject.toml</files>
  <read_first>backend/pyproject.toml</read_first>
  <action>
    Create or update `backend/pyproject.toml` with the pinned dependencies:
    - python = ">=3.14,<3.15"
    - fastapi = ">=0.136,<0.137"
    - uvicorn = {version = ">=0.49,<0.50", extras = ["standard"]}
    - pydantic = ">=2.13,<3.0"
    - sqlalchemy = ">=2.0.50,<2.1"
    - asyncpg = ">=0.31,<0.32"
    - alembic = ">=1.18,<1.19"
    - numpy = ">=2.4,<2.5"
    - scipy = ">=1.17,<1.18"
    - pandas = ">=3.0,<3.1"
    - pulp = ">=3.30,<4.0"
    - redis = {version = ">=8.0,<9.0", extras = ["hiredis"]}
    - httpx = ">=0.28,<0.29"
    Run `uv sync` to install dependencies and lock versions.
  </action>
  <acceptance_criteria>
    - backend/pyproject.toml contains "sqlalchemy = ">=2.0.50,<2.1"" and "asyncpg = ">=0.31,<0.32""
    - backend/uv.lock exists after sync
  </acceptance_criteria>
  <verify>test -f backend/uv.lock</verify>
  <done>Dependencies locked and synced via uv</done>
</task>

<task type="auto">
  <name>Set up Database Core connection and Async Session Management</name>
  <files>backend/app/core/database.py, backend/app/models/base.py</files>
  <read_first>backend/app/core/database.py</read_first>
  <action>
    Define the database URL: `postgresql+asyncpg://postgres:postgres@localhost:5432/ipl_draft`.
    Create `backend/app/core/database.py` with:
    - Async engine (`create_async_engine`) with pool configurations (`pool_size=10`, `max_overflow=20`, `pool_pre_ping=True`).
    - Async session helper (`async_sessionmaker(class_=AsyncSession)`).
    - Session generator dependency `get_db()`.
    Create `backend/app/models/base.py` declaring the SQLAlchemy declarative base class `Base`.
  </action>
  <acceptance_criteria>
    - database.py contains "create_async_engine" and "postgresql+asyncpg://"
    - base.py contains "class Base(DeclarativeBase):"
  </acceptance_criteria>
  <verify>python -c "import sys; sys.path.append('backend'); from app.core.database import engine; print(engine)"</verify>
  <done>Async engine initialized and base declarative model declared</done>
</task>

<task type="auto">
  <name>Design Declarative SQL Schemas for Players, Squads, Drafts, and Matches</name>
  <files>
    backend/app/models/player.py
    backend/app/models/squad.py
    backend/app/models/draft.py
    backend/app/models/match.py
  </files>
  <read_first>backend/app/models/base.py</read_first>
  <action>
    Create the model files and define SQLAlchemy mappings using type annotations (`Mapped`):
    - `player.py`: `Season`, `Franchise`, `Player`, `PlayerSeason`. Include: `balls_faced`, `runs_scored`, `balls_bowled`, `wickets_taken`, `percentile_batting`, `percentile_bowling`, `credit_cost`.
    - `draft.py`: `DraftSession` (user_id, status, budget_remaining, created_at) and `DraftPick` (draft_session_id, player_id, pick_number).
    - `match.py`: `MatchLog` (match_id, ball_number, over_number, batter_id, bowler_id, runs_scored, extras, wicket_type, event_meta as JSONB).
  </action>
  <acceptance_criteria>
    - player.py contains class mappings for Player and PlayerSeason
    - draft.py contains mappings for DraftSession and DraftPick
    - match.py contains mapping for MatchLog
    - All tables use appropriate foreign keys and indexes (e.g. unique constraints on Season year, Franchise code)
  </acceptance_criteria>
  <verify>python -c "import sys; sys.path.append('backend'); from app.models.player import Player; from app.models.draft import DraftSession; from app.models.match import MatchLog; print('Models loaded')"</verify>
  <done>All declarative models mapped for core entities</done>
</task>

<task type="auto">
  <name>Initialize and Configure Alembic Migrations</name>
  <files>backend/alembic.ini, backend/alembic/env.py</files>
  <read_first>backend/app/models/base.py</read_first>
  <action>
    Initialize Alembic in async mode: run `uv run alembic init -t async alembic` from `backend/` directory.
    Modify `backend/alembic/env.py` to:
    - Import `Base` from `backend/app/models/base.py` and register target metadata: `target_metadata = Base.metadata`.
    - Import all models so they are registered on `Base.metadata` for autogenerate.
    - Read database connection string from environment or defaults dynamically.
    Run `uv run alembic revision --autogenerate -m "Initial schema migration"` and apply it `uv run alembic upgrade head`.
  </action>
  <acceptance_criteria>
    - backend/alembic.ini exists
    - backend/alembic/env.py imports `Base.metadata`
    - At least one migration script exists in `backend/alembic/versions/`
  </acceptance_criteria>
  <verify>test -d backend/alembic/versions</verify>
  <done>Alembic async migrations configured and database upgraded to head</done>
</task>
</tasks>

<verification>
- [ ] uv run alembic upgrade head succeeds
- [ ] Database contains tables seasons, franchises, players, player_seasons, draft_sessions, draft_picks, match_logs
</verification>

<success_criteria>
- All database tables successfully created in PostgreSQL
- Schema correctly maps all requirements and constraints defined in CONTEXT.md
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-model-ingestion-engine/01-01-SUMMARY.md`
</output>
