---
phase: 01-data-model-ingestion-engine
plan: 01-03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - backend/tests/conftest.py
  - backend/tests/test_ingestion/test_db_schema.py
  - backend/tests/test_ingestion/test_normalizer.py
  - backend/tests/test_ingestion/test_matchups.py
autonomous: true
must_haves:
  truths:
    - "Test suite covers database connections and migrations"
    - "Test suite verifies pandas normalization output matches era relative percentile expectations"
    - "All ingestion and database unit/integration tests pass on local execution"
  artifacts:
    - path: "backend/tests/test_ingestion/test_normalizer.py"
      provides: "Unit tests verifying normalization math and thresholds"
    - path: "backend/tests/test_ingestion/test_db_schema.py"
      provides: "DB connectivity and schema mapping verification"
---

<objective>
Build a comprehensive test suite to verify the database schema, the pandas era-normalization logic, and the ingestion bulk loader operations.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-model-ingestion-engine/01-CONTEXT.md
@.planning/phases/01-data-model-ingestion-engine/01-RESEARCH.md
@.planning/phases/01-data-model-ingestion-engine/01-02-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Create Test Configuration and DB fixtures</name>
  <files>backend/tests/conftest.py, backend/tests/test_ingestion/test_db_schema.py</files>
  <read_first>backend/tests/conftest.py</read_first>
  <action>
    Create `backend/tests/conftest.py`.
    Set up standard pytest-asyncio event loop and session fixtures. Use a test database suffix (e.g. `ipl_draft_test`) or isolated transactions that roll back after test execution.
    Create `backend/tests/test_ingestion/test_db_schema.py` to assert that connections can be established and models can be queried successfully.
  </action>
  <acceptance_criteria>
    - conftest.py contains async database session fixture
    - test_db_schema.py contains test for executing a simple SELECT 1 or querying tables
  </acceptance_criteria>
  <verify>python -c "import sys; sys.path.append('backend'); import pytest; print('pytest imports')"</verify>
  <done>Pytest conftest and basic database connection tests initialized</done>
</task>

<task type="auto">
  <name>Create Normalization Pipeline Unit Tests</name>
  <files>backend/tests/test_ingestion/test_normalizer.py</files>
  <read_first>backend/app/ingestion/normalizer.py</read_first>
  <action>
    Create `backend/tests/test_ingestion/test_normalizer.py`.
    Write tests asserting that:
    - Players with >=30 balls faced receive relative percentiles within their season.
    - Players with <30 balls faced fallback to a percentile rating of 50.
    - Cost scaling accurately maps 99 to ~15.0 and 50 to ~6.0 credits.
  </action>
  <acceptance_criteria>
    - test_normalizer.py tests the output of `normalize_ratings_pipeline`
    - Tests include assertion of the 50 baseline fallback
    - Tests assert the credit cost calculation matches the mathematical model
  </acceptance_criteria>
  <verify>test -f backend/tests/test_ingestion/test_normalizer.py</verify>
  <done>ERA normalization and cost curve logic unit tests written</done>
</task>

<task type="auto">
  <name>Create Ingestion Matchup Verification Tests</name>
  <files>backend/tests/test_ingestion/test_matchups.py</files>
  <read_first>backend/app/ingestion/loader.py</read_first>
  <action>
    Create `backend/tests/test_ingestion/test_matchups.py`.
    Assert that the dynamic base matchup mapping and loader functions operate correctly, inserting seasons, franchises, players, and ratings into the database and verifying matchups via SELECT query joins.
  </action>
  <acceptance_criteria>
    - test_matchups.py tests the loader functionality
    - test_matchups.py performs database inserts and queries them back to verify matchups
  </acceptance_criteria>
  <verify>test -f backend/tests/test_ingestion/test_matchups.py</verify>
  <done>Dynamic matchup calculations and DB loading integration tests written</done>
</task>
</tasks>

<verification>
- [ ] uv run pytest backend/tests/ exits 0
</verification>

<success_criteria>
- The test suite is fully functional and passes
- All requirements DATA-01, DATA-02, DATA-03 are completely covered and verified by the test suite
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-model-ingestion-engine/01-03-SUMMARY.md`
</output>
